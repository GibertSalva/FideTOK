# Copiar a .env.local (gitignored) y reemplazar por tu endpoint Triton de devnet.
# Nunca commitear .env.local ni hardcodear estas URLs en el codigo.
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_WS_URL=wss://api.devnet.solana.com
